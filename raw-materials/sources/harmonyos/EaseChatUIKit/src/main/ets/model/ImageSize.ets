/**
 * 图片尺寸类。
 */
export class ImageSize {
  /**
   * 图片的宽度。
   */
  width: number;

  /**
   * 图片的高度。
   */
  height: number;

  constructor(width: number, height: number) {
    this.width = width;
    this.height = height;
  }

}