[**react-native-chat-sdk**](../README.md)

***

[react-native-chat-sdk](../globals.md) / ChatMessageEventListener

# Interface: ChatMessageEventListener

Defined in: ChatEvents.ts:573

The message event listener.

This listener listens for message state changes:

- If messages are sent successfully, a delivery receipt will be returned to the sender (the delivery receipt function needs to be enabled: [ChatOptions.requireDeliveryAck](../classes/ChatOptions.md#requiredeliveryack).

- If the recipient reads the received message, a read receipt will be returned to the sender (the read receipt function needs to be enabled: [ChatOptions.requireAck](../classes/ChatOptions.md#requireack))

During message delivery, the message ID will be changed from a local uuid to a global unique ID that is generated by the server to uniquely identify a message on all devices using the SDK.

Adds a message event listener:

  ```typescript
  let msgListener = new (class ss implements ChatMessageEventListener {
    onMessagesReceived(messages: ChatMessage[]): void {
      chatlog.log('ConnectScreen.onMessagesReceived', messages);
    }
    onCmdMessagesReceived(messages: ChatMessage[]): void {
      chatlog.log('ConnectScreen.onCmdMessagesReceived', messages);
    }
    onMessagesRead(messages: ChatMessage[]): void {
      chatlog.log('ConnectScreen.onMessagesRead', messages);
    }
    onGroupMessageRead(groupMessageAcks: ChatGroupMessageAck[]): void {
      chatlog.log('ConnectScreen.onGroupMessageRead', groupMessageAcks);
    }
    onMessagesDelivered(messages: ChatMessage[]): void {
      chatlog.log('ConnectScreen.onMessagesDelivered', messages);
    }
    onMessagesRecalledInfo(messages: ChatRecalledMessageInfo[]): void {
      chatlog.log('ConnectScreen.onMessagesRecalledInfo', messages);
    }
    onConversationsUpdate(): void {
      chatlog.log('ConnectScreen.onConversationsUpdate');
    }
    onConversationRead(from: string, to?: string): void {
      chatlog.log('ConnectScreen.onConversationRead', from, to);
    }
  })();
  ChatClient.getInstance().chatManager.addListener(msgListener);
  ```

Removes a message event listener:

  ```typescript
  ChatClient.getInstance().chatManager.delListener(this.msgListener);
  ```

## Methods

### onChatMessageThreadCreated()?

> `optional` **onChatMessageThreadCreated**(`event`): `void`

Defined in: ChatEvents.ts:657

Occurs when a message thread is created.

Each member of the group where the message thread belongs can call this method.

#### Parameters

##### event

[`ChatMessageThreadEvent`](../classes/ChatMessageThreadEvent.md)

The message thread creation event.

#### Returns

`void`

***

### onChatMessageThreadDestroyed()?

> `optional` **onChatMessageThreadDestroyed**(`event`): `void`

Defined in: ChatEvents.ts:675

Occurs when a message thread is destroyed.

Each member of the group where the message thread belongs can call this method.

#### Parameters

##### event

[`ChatMessageThreadEvent`](../classes/ChatMessageThreadEvent.md)

The message thread destruction event.

#### Returns

`void`

***

### onChatMessageThreadUpdated()?

> `optional` **onChatMessageThreadUpdated**(`event`): `void`

Defined in: ChatEvents.ts:667

Occurs when a message thread is updated.

This method is triggered when the message thread name is changed or a message in the message thread is updated.

Each member of the group where the message thread belongs can call this method.

#### Parameters

##### event

[`ChatMessageThreadEvent`](../classes/ChatMessageThreadEvent.md)

The message thread update event.

#### Returns

`void`

***

### onChatMessageThreadUserRemoved()?

> `optional` **onChatMessageThreadUserRemoved**(`event`): `void`

Defined in: ChatEvents.ts:681

Occurs when the current user is removed from the message thread by the admin.

#### Parameters

##### event

[`ChatMessageThreadEvent`](../classes/ChatMessageThreadEvent.md)

The message thread user removal event.

#### Returns

`void`

***

### onCmdMessagesReceived()?

> `optional` **onCmdMessagesReceived**(`messages`): `void`

Defined in: ChatEvents.ts:590

Occurs when a command message is received.

Unlike [onMessagesReceived](#onmessagesreceived), this callback only contains a command message body that is usually invisible to users.

#### Parameters

##### messages

[`ChatMessage`](../classes/ChatMessage.md)[]

The received command message(s).

#### Returns

`void`

***

### onConversationRead()?

> `optional` **onConversationRead**(`from`, `to?`): `void`

Defined in: ChatEvents.ts:641

Occurs when a conversation read receipt is received.

This callback occurs in either of the following cases:

- The message is read by the recipient and the conversation read receipt is sent. Upon receiving this event, the SDK sets the `isAcked` property of the message in the conversation to `true` in the local database.

- In a multi-device login scenario, when one device sends a conversation receipt, the server will set the number of unread messages to `0`, call the callback method on the other devices, and set the `isRead` property of the message in the conversation to `true` in the local database.

#### Parameters

##### from

`string`

The user who sends the conversation read receipt.

##### to?

`string`

The user who receives the conversation read receipt.

#### Returns

`void`

***

### onConversationsUpdate()?

> `optional` **onConversationsUpdate**(): `void`

Defined in: ChatEvents.ts:627

Occurs when the conversation is updated.

This callback is triggered only when a conversation is added or removed.

#### Returns

`void`

***

### onGroupMessageRead()?

> `optional` **onGroupMessageRead**(`groupMessageAcks`): `void`

Defined in: ChatEvents.ts:604

Occurs when a read receipt is received for a group message.

#### Parameters

##### groupMessageAcks

[`ChatGroupMessageAck`](../classes/ChatGroupMessageAck.md)[]

The read receipt(s) for group message(s).

#### Returns

`void`

***

### onMessageContentChanged()?

> `optional` **onMessageContentChanged**(`message`, `lastModifyOperatorId`, `lastModifyTime`): `void`

Defined in: ChatEvents.ts:690

Occurs when the content of a text message is modified.

#### Parameters

##### message

[`ChatMessage`](../classes/ChatMessage.md)

The modified message.

##### lastModifyOperatorId

`string`

The user ID of the operator that modified the message last time.

##### lastModifyTime

`number`

The last message modification time. It is a UNIX timestamp in milliseconds.

#### Returns

`void`

***

### onMessagePinChanged()?

> `optional` **onMessagePinChanged**(`params`): `void`

Defined in: ChatEvents.ts:704

Occurs when the message pinning status changed.

#### Parameters

##### params

###### convId

`string`

###### messageId

`string`

###### pinInfo

[`ChatMessagePinInfo`](../classes/ChatMessagePinInfo.md)

###### pinOperation

`number`

#### Returns

`void`

#### Params

params -
- Param [messageId] The ID of the message with the changed pinning status.
- Param [convId] The ID of the conversation that contains the message.
- Param [pinOperation] The message pinning or unpinning operation.
- Param [pinInfo] The information about message pinning or unpinning, including the operation time and the user ID of the operator. See [ChatMessagePinInfo](../classes/ChatMessagePinInfo.md).

***

### onMessageReactionDidChange()?

> `optional` **onMessageReactionDidChange**(`list`): `void`

Defined in: ChatEvents.ts:648

Occurs when a message reaction changes.

#### Parameters

##### list

[`ChatMessageReactionEvent`](../classes/ChatMessageReactionEvent.md)[]

The reaction change event.

#### Returns

`void`

***

### onMessagesDelivered()?

> `optional` **onMessagesDelivered**(`messages`): `void`

Defined in: ChatEvents.ts:611

Occurs when a delivery receipt is received.

#### Parameters

##### messages

[`ChatMessage`](../classes/ChatMessage.md)[]

The message(s) for which delivery receipt(s) is sent.

#### Returns

`void`

***

### onMessagesRead()?

> `optional` **onMessagesRead**(`messages`): `void`

Defined in: ChatEvents.ts:597

Occurs when a read receipt is received for a message.

#### Parameters

##### messages

[`ChatMessage`](../classes/ChatMessage.md)[]

The read receipt(s).

#### Returns

`void`

***

### onMessagesRecalledInfo()?

> `optional` **onMessagesRecalledInfo**(`info`): `void`

Defined in: ChatEvents.ts:620

Occurs when a received message is recalled.

If the recipient is offline when the message is delivered and recalled, the recipient only receives this callback instead of the message.

#### Parameters

##### info

[`ChatRecalledMessageInfo`](../classes/ChatRecalledMessageInfo.md)[]

#### Returns

`void`

***

### onMessagesReceived()?

> `optional` **onMessagesReceived**(`messages`): `void`

Defined in: ChatEvents.ts:581

Occurs when a message is received.

This callback is triggered to notify the user of the message that is received, such as texts or an image, video, voice, location, or file.

#### Parameters

##### messages

[`ChatMessage`](../classes/ChatMessage.md)[]

The received message(s).

#### Returns

`void`
